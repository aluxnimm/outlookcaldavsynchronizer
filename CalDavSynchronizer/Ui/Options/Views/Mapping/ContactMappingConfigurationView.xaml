<UserControl x:Class="CalDavSynchronizer.Ui.Options.Views.Mapping.ContactMappingConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mapping="clr-namespace:CalDavSynchronizer.Ui.Options.ViewModels.Mapping"
             mc:Ignorable="d"
             d:DataContext="{x:Static mapping:ContactMappingConfigurationViewModel.DesignInstance}">

  <StackPanel>
    <CheckBox IsChecked="{Binding MapBirthday}">Map Birthday</CheckBox>
    <CheckBox IsChecked="{Binding MapContactPhoto}">Map Contact Photo</CheckBox>
    <CheckBox IsChecked="{Binding FixPhoneNumberFormat}">Fix imported phone number format</CheckBox>
  </StackPanel>

</UserControl>